# Replace this line with your custom domain, e.g.:
# o2elite.yourdomain.com
#
# Then go to your GitHub repo → Settings → Pages → Custom domain
# and enter the same value. Delete this comment block and keep only
# the domain name on a single line if using a custom domain.
# If not using a custom domain, delete this file entirely.
